package com.example.classclasstufftuff

fun main () {
    var name = "prabhash" //Immutable (cannot be changed)
    val age = 19          // Mutable (can be changed)

    print("My name is $name and i am $age years old")
}